import styled from "styled-components";

export const Container = styled.div`
margin-bottom: 20px;
display: flex;
flex-direction: column;
> p{
    margin: 0;
    color: var(--gray);
}
> span{
    color: var(--gray);
    margin-bottom: 3px;
}
> strong{
    font-size: 17px;
}
` 